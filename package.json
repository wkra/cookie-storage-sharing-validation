{
  "name": "cookie-storage-sharing-validation",
  "version": "1.0.0",
  "description": "Cookie And Storage Sharing Validation",
  "author": "wkrawiec",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "test": "jest",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup",
    "generate:gh-pages": "cross-env DEPLOY_ENV=GH_PAGES nuxt generate"
  },
  "dependencies": {
    "nuxt": "^2.0.0",
    "@nuxtjs/pwa": "^3.0.0-0"
  },
  "devDependencies": {
    "@vue/test-utils": "^1.0.0-beta.27",
    "babel-jest": "^24.1.0",
    "cross-env": "^6.0.3",
    "gh-pages": "^2.1.1",
    "jest": "^24.1.0",
    "push-dir": "^0.4.1",
    "vue-jest": "^4.0.0-0"
  }
}
